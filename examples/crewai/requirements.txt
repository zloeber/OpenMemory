# Requirements for CrewAI Flow Launcher Web UI

# Core dependencies (should already be installed for launch_flow.py)
crewai>=0.86.0
pydantic>=2.0.0
pyyaml>=6.0
click>=8.0.0

# MCP support
mcp>=1.0.0
httpx>=0.27.0

# Web UI dependencies
gradio>=4.0.0

# Optional validation
jsonschema>=4.0.0
